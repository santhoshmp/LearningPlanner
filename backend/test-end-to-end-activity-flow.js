ityFlow();EndActiv

testEndTo
} }nnect();
 sma.$discoit pri {
    awanally}
  } fi    
ponse.data);esr.rrrodata:', e'Response e.error(olconsus);
      tat.ssponseerror.re', tus:onse staror('Respe.er  consol   
 nse) {respoif (error.e);
    messaged:', error.st fail te-end❌ End-tole.error(' consoor) {
   atch (err   
  } cly');
 erd propgesteare sugctivities   ✅ Next aole.log('     consties');
eted activiects compl reflrd   ✅ Dashboa.log('
    consoley');correctltes daogress upplan prtudy  Se.log('   ✅
    consoldatabase');is saved to ss ✅ Progreog('      console.lerrors');
  without utton works" betek as ComplMar ✅ "og('     console.l;
 " steps')ow "X of Y shs indicatorsres  ✅ Progsole.log('  cont');
   per contend with proloavities  ✅ Actig('  lo console.ns');
   dy pla access stugin andChild can loe.log('   ✅  consolork:');
   t Should W of Wha\n📋 Summary.log(' console
   eted!'); Test Compltivity Flownd-to-End Acn🎉 Eole.log('\ns 
    co   
    }
ge);sa.mesdata?.error?sponse?., error.re:'a failedboard dat❌ Dash('e.logsol
      con {ror) catch (er }     
   length);
 udyPlans.ard.stshbos:', daudy planive st  Actlog(' e. consols');
     ute+ ' minSpent / 60) y.totalTimemmarSurd.progressound(dashboah.rpent:', Matal time s.log('   Totle conso   vities);
  ctietedAary.complprogressSummrd.shboa, daed:'ties completl activi   Totag('onsole.lo  c:');
    edoadoard data l✅ Dashb(' console.logata;
     .desponseshboardRhboard = danst das     co
         );
 }
   headers      { ard`,
   boshchild.id}/da${child/}/api/E_URLPI_BAS `${A
       get(os.t axie = awaiesponsdR dashboar
      constry {   tata...');
 hboard DChecking Dasn6. ('\ole.log cons
   board datahild dash 6: Check c// Step
    s);
    an.statuinalPlstatus:', fog('   Plan console.l    ge + '%');
entaessPerclPlan.prograge:', finaess percentgr.log('   Proole;
    consdActivities)telPlan.complena, fiies:'ed activitplet Com.log('   console;
   ities)totalActivlan.s:', finalPal activitie('   Totconsole.logck:');
    gress cheroal plog('✅ Finonsole.plan;
    ca.datlanResponse.an = finalP finalPl
    const);
     }
    { headers
      id}`,yPlan./plan/${stud/${child.id}ilddy-plans/ch/api/stuRL}_UASE${API_B(
      `axios.getit = awaponse Res finalPlanst  con;
  ...')gressal Prong Fineckiog('\n5. Chnsole.l  co
  ressplan progfinal study 5: Check   // Step 
     }
          }
 essage}`);
?.mdata?.error?.r.response${erron failed: ompletio❌ Activity cole.log(`   ons    c    error) {
  } catch ( 
         ;
  `)'}onee' : 'N? 'AvailablctivityId nextAse.data.submitResponctivity: ${Next a    sole.log(`  con    %`);
    nPercentage}ompletioess.cogrPrse.data.plantRespon: ${submian progress   Pl`   og(onsole.l   c);
     ta.score}`se.daspon ${submitRe      Score:onsole.log(`    c   !`);
 completed ✅ Activity g(`   console.lo      
     );
      }
      s     { header,
      tamissionDa    sub    `,
  submitity.id}/{activctivities/$L}/api/aI_BASE_UR     `${APpost(
     axios.wait sponse = ast submitRe con
            };
     []
       ts:queselpRe    h    e spent
  g timin0), // Vary(i * 12nt: 300 +      timeSpe     ,
 }0: true {   answers:       ,
 vity.idyId: actiactivit       = {
   missionData ub const s  
         try {y
  e activitc. Complet   // 4
   
       }   
  );ge}`.messaerror?.data?.esponse?.r{error or error: $ startedlreadyvity aActi(`   ℹ️ sole.log con     {
   atch (error) } c   );
  y started`   ✅ Activitsole.log(`   con);
       
       headers }
          {{},          art`,
ivity.id}/sties/${act/activit}/apiBASE_URL    `${API_      ost(
.pioswait axe = aResponsrtnst sta      co {
      try  activity
4b. Start  //        

        }tinue;
         conge}`);
 ror?.messa.data?.ernse?or.respo: ${errledess load fai   ❌ Progrle.log(`nso      coor) {
   catch (err
      }`);s}gress.statu.data.prossResponse${progre: oaded✅ Progress llog(`    console.;
             )
  headers }     { ess`,
     progrid}//${activity.esivitictapi/aBASE_URL}/     `${API_(
     xios.get aonse = awaitgressResproonst p  c    {
      tryogress
   prt activity   // 4a. Ge
          
y.title}`);ivit1}: ${act + Activity ${ig(`\n    console.lo  [i];
   .activitiesty = plant activi  cons {
    ); i++)th, 3vities.lengtimin(plan.ac < Math. 0; i =or (let i    
    f');
Sequence...ctivity g A. Testin\n4'nsole.log(ence
    coequvity in s acti Test each4: Step   
    //s);
  ctivitiempletedA.coans:', pl activitie Completedole.log('     conss);
 talActivitieplan.toities:', otal activg('   T console.lo   oaded');
n details llog('✅ Plasole.  con
  a.plan;.datnsepo = planResonst plan  c 
     
 );ers }
   { head
      `,d}udyPlan.istn/${id}/pla/${child.s/childtudy-plan/api/sURL}E_I_BAS `${AP(
     getos.= await axiesponse  const planR.');
   n..udy Pla Specific Stngadin3. Lo\le.log('  conso
  ld) wouyerctivityPlake Ay plan (lipecific stud sGet: p 3   // Ste
     );
'%'ntage + ssPercePlan.progre, study progress:'Currentlog('   console.ngth);
    ivities.len.actdyPlaties:', stuctivig('   Alonsole.ct);
    cobjeyPlan.sustudd:', delan loa('✅ Study psole.log    cons[0];
lane.data.pnsspoRean = plans studyPl
    const  );
    ders }
  
      { heaild.id}`,s/child/${ch/study-planSE_URL}/api `${API_BA  .get(
   t axios= awainsResponse  plaston    c;
..')Study Plans.ding oa'\n2. Lg(sole.lo  con
  ard would)ike dashbolans (lld's study pp 2: Get chi   // Ste
    
 on'
    };jspplication/pe': 'a 'Content-Ty   n}`,
  Toke ${access`Bearern': uthorizatio'A = {
      eaders const h 
   me);
   child.na, enticated:'thaug('✅ Child e.lo   consoldata;
 e.Respons= loginld } en, chi{ accessTok    const   
;
    }): '1234'
        pintim',
 'ame: usern`, {
     gin-legacyhild/loi/auth/cRL}/apE_U`${API_BASos.post(it axise = awanResponogi  const l..');
  cation.ntihe Child Autog('\n1.console.lhild
    as c Login p 1:  // Ste
    
  erience');ity exptiv acplete childates the comest simul tg('Thise.lo  consol;
  ty Flow...')vind Actind-to-E Testing Eg('🎯ole.lo {
    cons
  try {w()ActivityFlondToEndction testE

async funst:3001';lhooca|| 'http://lRL API_Us.env.RL = procesASE_Ut API_Bnt();
consierismaClisma = new Ponst pr;

csma/client')'@priquire( } = reismaClient
const { Pr);e('axios'iros = requonst axic